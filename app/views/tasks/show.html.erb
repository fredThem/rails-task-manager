<div>
  <h1><%= @task.title %></h1>
  <p role="details"> <%= @task.details %></p>
  <p><%= @task.completed? ? "☑ This task is completed" : "☐ This task is incomplete"  %></p>
 
  <button role="goBack">
    <%= link_to "back to list", tasks_path %>
  </button>
</div>
